[project]
name = "ha-meridian-energy"
version = "1.2.0"
description = "Meridian Energy integration for Home Assistant"
requires-python = ">=3.14"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

